---
import type {CommandPaletteAction as Props} from '../types';

const {id, selected = false} = Astro.props;
---

<button
  id={id} 
  tabindex="0" 
  class="command-palette-action"
  data-selected={selected.toString()}>
  <span>
    <slot />
  </span>
</button>

<style>
  a {
    width: 100%;
    height: 3rem;
    display: flex;
    padding-left: 1rem;
    align-items: center;
    text-decoration: none;
</style>
